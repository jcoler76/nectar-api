# Admin Backend Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=4001

# Main API Configuration (for backend communication)
MAIN_API_URL=http://localhost:3001
MAIN_API_KEY=your-secure-api-key-for-service-to-service-auth

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
# Note: Publishable key and webhook secret are configured via admin interface

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3002,https://admin.yourdomain.com

# JWT Secret for admin authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin User Configuration (for seeding)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=change-this-password

# Service API Keys for internal communication (REQUIRED)
MARKETING_SERVICE_API_KEY=your-secure-marketing-service-api-key-here

# NOTE: Admin-backend does NOT use DATABASE_URL anymore
# All database access goes through MAIN_API_URL endpoints
# This ensures single schema, no drift, and proper RLS enforcement